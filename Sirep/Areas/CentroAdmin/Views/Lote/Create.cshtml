@model Sirep.Models.Lote

@{
    ViewData["Title"] = "Crear Lote";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col-sm-12 offset-lg-3 col-lg-6">
        <div class="card shadow">
            <div class="card-header text-center">
                <h5 class="m-0">Crear Lote</h5>
            </div>
            <div class="card-body">
                <form asp-action="Create" autocomplete="off">
                    @Html.AntiForgeryToken()
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <input type="hidden" asp-for="CentroId" value="@ViewBag.CentroId" />
                    <div class="form-group">
                        <label asp-for="Nombre" class="control-label"></label>
                        <input asp-for="Nombre" class="form-control" />
                        <span asp-validation-for="Nombre" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="Detalles" class="control-label"></label>
                        <input asp-for="Detalles" class="form-control" />
                        <span asp-validation-for="Detalles" class="text-danger"></span>
                    </div>
                    <div class="form-group text-center m-0">
                        <input type="submit" value="Crear" class="btn btn-primary px-4" />
                    </div>
                </form>
            </div>
        </div>
        <div class="text-center my-4">
            <a href="/Centro/Lotes/@ViewBag.CentroId" class="btn btn-outline-secondary rounded-pill"><i class="fas fa-arrow-left"></i> Volver al Listado</a>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

